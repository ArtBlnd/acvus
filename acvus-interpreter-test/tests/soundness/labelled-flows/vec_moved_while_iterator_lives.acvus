// refuse
// The iterator holds the vector's loan: moving the vector while the iterator is used after is refused.
let v = vec([5, 3]); let it = as_iter(&v); let w = v; let a = next(&mut it); *a.unwrap() + len(&w)
