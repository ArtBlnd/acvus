for slice(r8) -> L1 else L2 stages [L1, L3, L4, L5]
// L1: free {const_str, const}
// L3: cycle Storage(r11) in_order {ref, call to_string, take, +, assign}
// L4: free {assign, ref, concat, drop, assign, ref, concat, drop, assign, ref, as_slice}
// L5: cycle Order(r10) in_order {spawn print, eval}
// control upfront
