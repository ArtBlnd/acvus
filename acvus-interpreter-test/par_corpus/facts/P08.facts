for slice(r4) -> L1 else L2 stages [L1, L5]
// L1: free {take, const, ==}
// L5: cycle Carried(r6) any_order law(Op(Xor) exact commutative) {!, branch, jump}
// control upfront
// cost W=4 O=18281 split when n > 146248, n = len(r4)
