{
  "template": "{{ x = as_iter(&@users) | map(|u| -> clone(&u.name)) | collect }}{{ into_iter(x) | join(\", \".to_string()) }}",
  "context": {
    "users": [
      { "name": "alice", "age": 30 },
      { "name": "bob", "age": 25 }
    ]
  },
  "expected": "alice, bob"
}
