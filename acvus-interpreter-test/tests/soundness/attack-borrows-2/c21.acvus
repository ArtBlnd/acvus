// refuse
// a lambda capturing a & returned from an if join lives past a push
let v = vec([1]); let w = vec([2]); let s = &v; let t = &w; let f = if true { |k| -> s.len() + k } else { |k| -> t.len() + k }; v.push(1); f(0u64)
