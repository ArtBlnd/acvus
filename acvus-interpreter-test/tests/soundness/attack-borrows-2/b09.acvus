// expect 4
// a join of two &mut in a loop, each iteration pushing through the join
let a = vec([1]); let b = vec([1]); let i = 0; while i < 2 { let r = if i == 0 { &mut a } else { &mut b }; r.push(i); i = i + 1; } a.len() + b.len()
