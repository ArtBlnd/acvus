{
  "template": "% let x = as_iter(&@items) | map(|i| -> *i) | filter(|x| -> *x != 0) | collect\n{{ into_iter(x) | map(|i| -> i.to_string()) | join(\", \".to_string()) }}",
  "context": { "items": [0, 1, 2, 0, 3] },
  "expected": "1, 2, 3"
}
