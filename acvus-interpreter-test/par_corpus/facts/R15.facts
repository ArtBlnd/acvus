for slice(r14) -> L1 else L2 stages [L1]
// L1: cycle Storage(r17) in_order law(Reset(Op(Concat)) exact) {assign, ref, concat, assign, ref, concat, assign}
// control upfront
// cost in place: no stage runs apart
