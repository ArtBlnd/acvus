for slice(r17) -> L1 else L2 stages [L1, L7]
// L1: free {as_slice}
// L7: cycle Order(r19) any_order law(Order exact commutative) {merge, jump, jump}
// control upfront
// cost W=3 O=18281 split when n > 194998, n = len(r17)
for slice(r22) -> L4 else L5 stages [L4, L6]
// L4: free {take, assign, ref, call to_string, assign, ref, as_slice, spawn print, eval, drop}
// L6: cycle Order(r23) any_order law(Order exact commutative) {merge}
// control upfront
// cost W=18294 O=18281 split when n > 32, n = len(r22)
