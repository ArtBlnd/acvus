{
  "template": "{{ result = @matrix | into_iter | flatten_arrays | fold(0, |acc, x| -> acc + x) }}{{ result | to_string }}",
  "context": {
    "matrix": [[1, 2], [3, 4], [5, 6]]
  },
  "expected": "21"
}
