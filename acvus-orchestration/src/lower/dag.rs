// DAG construction is handled by CompilationGraph::resolve() in acvus-mir.
// No separate DAG module needed — the graph engine discovers dependencies
// from Entity constraints (Derived, ExternUnit refs, MembershipId).
