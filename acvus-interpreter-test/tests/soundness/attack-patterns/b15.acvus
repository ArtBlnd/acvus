// expect 6
// while let with tuple-with-Some pattern over pops; stops at (2, None)
let v = vec([(1, Some(1)), (2, None), (3, Some(3))]); let s = 0; while let Some((a, Some(b))) = v.pop() { s = s + a + b; } s
