// expect 3: while let over an iterator of references, two parts matched in the body
let v = vec([(1, 2)]); let it = as_iter(&v); let n = 0; while let Some(x) = next(&mut it) { n = match x { (a, b) => *a + *b, _ => 0, }; } n
