for slice(r6) -> L1 else L2 stages [L1, L3]
// L1: free {take, assign, ref, call to_string, assign, ref, as_slice}
// L3: cycle Order(r8) in_order {spawn print, eval}
// control upfront
// cost W=19 O=18281 split when n > 30790, n = len(r6)
