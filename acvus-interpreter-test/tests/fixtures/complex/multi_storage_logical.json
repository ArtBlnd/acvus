{
  "template": "{{ filtered = @items | filter(|i| -> i >= @min && i <= @max) | collect }}{{ filtered | len | to_string }}{{_}}{{/}}",
  "context": { "items": [3, 7, 1, 9, 4], "min": 2, "max": 8 },
  "expected": "3"
}
