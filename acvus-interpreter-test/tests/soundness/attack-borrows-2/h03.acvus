// expect 3
// an inlined parameter's field lent, the argument an object from a match
let k = 1; let f = |p| -> { let r = &p.v; len(r) }; f(match k { 0 => { v: vec([1]), }, _ => { v: vec([1, 2, 3]), } })
