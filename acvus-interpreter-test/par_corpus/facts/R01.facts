for slice(r8) -> L1 else L2 stages [L1, L5]
// L1: free {take, const, >}
// L5: cycle Carried(r10) any_order law(Op(Add) exact commutative) {const, +, branch, jump}
// control upfront
// cost W=4 O=18281 split when n > 146248, n = len(r8)
