// refuse
// a join of &mut and & carried across loop iterations while the owner is written
let a = vec([1]); let b = vec([1]); let r = &mut a; let s = &b; let i = 0; while i < 2 { s = if i == 0 { r } else { &b }; a.push(1); i = i + 1; } s.len()
