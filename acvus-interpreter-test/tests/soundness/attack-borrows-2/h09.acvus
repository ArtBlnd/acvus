// expect "ab"
// an inlined parameter named as storage through a nested field, argument strings from opaque-free concatenations
let f = |p| -> { let r = &p.x.y; r.to_string() + p.x.z }; f({ x: { y: "a" + "", z: "b" + "", }, })
