for slice(r7) -> L1 else L2 stages [L1, L3]
// L1: free {take, const, *, assign, ref, call to_string, assign, ref, as_slice, spawn print}
// L3: cycle Order(r9) any_order law(Order exact commutative) {eval, merge}
// control upfront
