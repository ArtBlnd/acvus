// expect 80
// Two pulls of one iterator over a map's values, both read after the second (RFC-0096).
let m = hash_map(); insert(&mut m, 5, 50); insert(&mut m, 3, 30); let it = values(&m); let a = next(&mut it); let b = next(&mut it); *a.unwrap() + *b.unwrap()
