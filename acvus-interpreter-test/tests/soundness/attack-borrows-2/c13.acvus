// expect 3
// anyorder inside a loop: the & made before the loop is overwritten before any read, so it is dead at the push
let v = vec([1]); let s = &v; let i = 0; while i < 2 { anyorder { v.push(1); } s = &v; i = i + 1; } s.len() + 0
