for slice(r31) -> L1 else L2 stages [L1, L8]
// L1: free {ref, const_str, string_eq, !}
// L8: cycle Storage(r32) in_order {ref, as_slice, call concat, assign, ref, as_slice, call concat, assign, ref, as_slice, call concat, assign, ref, as_slice, call concat, assign}
// L8: cycle Storage(r33) any_order law(Op(Add) exact commutative) {take, +, assign}
// control upfront
// cost W=5 O=18281 split when n > 116999, n = len(r31)
