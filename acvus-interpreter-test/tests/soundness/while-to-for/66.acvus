// expect 105
let xs = vec([5, 3, 9, 8, 7]); let i = 0u64; let found = false; while i < xs.len() && !found { if xs[i] < 0 { found = true; } else { i = i + 1u64; }; } if found { i } else { i + 100u64 }
