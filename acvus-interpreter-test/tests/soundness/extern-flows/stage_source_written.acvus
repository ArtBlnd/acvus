// refuse
// The stage holds its source's reference: the vector it borrows is written while the stage is live.
let v = vec([1, 2, 3]); let w = vec([5]); let m = as_iter(&v) | map(|x| -> *x + len(&w)); v = vec([9]); m | fold(0, |a, x| -> a + x)
