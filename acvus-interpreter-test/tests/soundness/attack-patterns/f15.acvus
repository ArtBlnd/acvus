// expect 3: for over &mut v, pattern tests element through &mut, then write element directly
let v = vec([Some(1), None, Some(2)]); let s = 0; for e in &mut v { if let Some(x) = e { s = s + *x; }; } s
