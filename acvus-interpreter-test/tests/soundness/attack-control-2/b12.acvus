// expect 3
let acc = 0; for s in ["a".to_string(), "bb".to_string(), "c".to_string()] { let j = 0; while j < 3 { if j == 1 { break; } j = j + 1; } acc = acc + j; if len(&s) == 2 { break; } acc = acc + len(&s); } acc
