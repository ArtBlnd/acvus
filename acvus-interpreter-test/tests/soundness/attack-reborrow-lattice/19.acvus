// expect 121
let a = vec([1]); let b = vec([2]); let e = vec([3]); let c = true; let d = false; let r = if c { if d { &mut a } else { &mut b } } else { &mut e }; r.push(1); a.len() * 100 + b.len() * 10 + e.len()
