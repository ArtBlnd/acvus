// expect unwrap: called on None
// `read_later` is spawned lent `&n` and `unwrap` of `None` traps before its `Eval`.
let n = 41; let a = [1, 2, 3]; let j = a.len() - a.len() + 5; let m = read_later(&n); *a.get(j).unwrap() + m
