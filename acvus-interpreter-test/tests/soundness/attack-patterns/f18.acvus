// expect "a": object pattern open matching (extra fields ignored) on a lambda result
let f = |x| -> { k: x, extra: 1, other: "o".to_string(), }; match f("a".to_string()) { { k, } => k, _ => "".to_string(), }
