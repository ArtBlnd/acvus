[translate] {{ @translate | map(|m| -> m.content) | join("".to_string()) }}
[sentiment] {{ @sentiment | map(|m| -> m.content) | join("".to_string()) }}
[chat] {{ @chat | map(|m| -> m.content) | join("".to_string()) }}