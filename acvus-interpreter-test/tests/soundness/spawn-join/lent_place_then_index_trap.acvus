// expect index out of bounds: the len is 3 but the index is 5
// `read_later` is spawned lent `&n` and the index traps before its `Eval`: the task reads `n` after the run has ended.
let n = 41; let v = vec([1, 2, 3]); let j = v.len() - v.len() + 5; let m = read_later(&n); v[j] + m
