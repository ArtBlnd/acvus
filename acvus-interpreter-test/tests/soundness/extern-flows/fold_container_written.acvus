// refuse
// fold's result keeps a reference to an element: the vector is written while the result is live.
let v = vec([1, 2]); let r = as_iter(&v) | fold(None, |a, x| -> Some(x)); v = vec([3]); match r { Some(p) => *p, None => 0, }
