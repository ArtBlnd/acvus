// expect 1,3,7,15,31,63,127,255
let xs = vec([1u8, 1u8, 1u8, 1u8, 1u8, 1u8, 1u8, 1u8]);
let y = 0u8;
let out = vec([]);
for x in &xs {
    y = 2u8 * y + *x;
    out.push(y);
}
out.into_iter().map(|x| -> x.to_string()).join(",".to_string())
