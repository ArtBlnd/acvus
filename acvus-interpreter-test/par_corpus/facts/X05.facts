for slice(r3) -> L1 else L2 stages [L1, L3]
// L1: free {call to_string, assign, ref, take, assign, ref, call to_string, assign, ref, concat, drop, drop, assign, ref, as_slice, spawn print}
// L3: cycle Order(r6) any_order law(Order exact commutative) {eval, merge}
// control upfront
