// expect 25: for over array by value of tuples of Vecs, move Vec out by pattern
let a = [(vec([1, 2]), 0), (vec([3, 4, 5]), 1)]; let s = 0u64; for e in a { match e { (w, 0) => { s = s + w.len() * 10u64; }, (w, _) => { s = s + w.len() - 3u64 + 5u64; }, _ => { let z = 0; }, }; } s
