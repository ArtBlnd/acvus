// expect 3
// a &mut and a & meet at an if as a &; the &mut is used after the join's last use
let v = vec([1]); let w = vec([5, 6]); let c = false; let r = &mut v; let s = if c { r } else { &w }; let n = s.len(); r.push(2); n + r.len() - 1
