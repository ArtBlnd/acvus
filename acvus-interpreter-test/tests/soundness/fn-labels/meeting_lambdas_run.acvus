// expect 2
// The met lambdas without a write.
let v = [7, 8]; let w = [9, 10]; let c = len(&v) > 1; let f = |a, b| -> a; let g = |a, b| -> b; let h = if c { f } else { g }; let x = h(&v, &w); len(x)
