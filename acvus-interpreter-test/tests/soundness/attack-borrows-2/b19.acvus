// expect 2
// a join whose arms are both & of one storage and a &mut reborrow after it dies
let v = vec([1, 2]); let c = true; let s = if c { &v } else { &v }; let n = s.len(); let r = &mut v; r.push(3); n + v.len() - 3
