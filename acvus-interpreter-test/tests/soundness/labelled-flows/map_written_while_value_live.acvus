// refuse
// The value holds the map's loan: an insert while it is live is refused.
let m = hash_map(); insert(&mut m, 5, 50); let it = values(&m); let a = next(&mut it); insert(&mut m, 1, 10); *a.unwrap()
