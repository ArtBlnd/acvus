// refuse
// The lender the result of trim_matches borrows is written while the result is live.
let a = "hello".to_string(); let b = "x".to_string(); let t = trim_matches(&a, &b); a = "y".to_string(); len(t)
