{
  "template": "{{ x = as_iter(&@items) | map(|i| -> *i + 1) | collect }}{{ x | map(|i| -> i.to_string()) | collect | join(\", \".to_string()) }}",
  "context": { "items": [1, 2, 3] },
  "expected": "2, 3, 4"
}
