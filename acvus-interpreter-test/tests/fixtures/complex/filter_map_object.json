{
  "template": "% let x = as_iter(&@products) | filter(|p| -> (*p).price >= 100) | map(|p| -> clone(&p.name)) | collect\n{{ into_iter(x) | join(\", \".to_string()) }}",
  "context": {
    "products": [
      { "name": "apple", "price": 100 },
      { "name": "banana", "price": 50 },
      { "name": "cherry", "price": 200 }
    ]
  },
  "expected": "apple, cherry"
}
