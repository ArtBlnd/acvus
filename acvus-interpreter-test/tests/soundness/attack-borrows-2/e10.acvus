// expect 2
// a &mut and a & to different vecs are joined, then the join is read as a view
let v = vec([1]); let w = vec([2, 3]); let r = &mut v; let s = if false { r } else { &w }; s.as_slice().len()
