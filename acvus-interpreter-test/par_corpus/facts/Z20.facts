for slice(r14) -> L1 else L2 stages [L1, L5]
// L1: free {take}
// L5: cycle Order(r16)+Storage(r17) in_order {take, >, take, assign, ref, call to_string, assign, ref, as_slice, spawn print, eval, branch, jump}
// control upfront
// cost W=3 O=18281 split when n > 194998, n = len(r14)
