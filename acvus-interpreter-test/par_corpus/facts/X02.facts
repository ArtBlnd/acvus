for slice(r4) -> L1 else L2 stages [L1, L3]
// L1: free {take, assign, ref, call to_string, assign, ref, const_str}
// L3: cycle Storage(r5) in_order law(Op(Concat) exact) {ref, concat, assign, ref, concat, assign}
// control upfront
