// expect 4.875
// Decimal * via lambda by &, operands still readable
let x = decimal("1.5".to_string()).unwrap(); let y = decimal("2.25".to_string()).unwrap(); let f = |a, b| -> a * b; let r = f(&x, &y); (r + x).to_string()
