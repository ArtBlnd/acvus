// expect "5,8,1,0,0"
let xs = vec([5, 3, 8, 1]);
let out = vec([]);
let pos = vec([0, 0, 0, 0, 0]);
for x in &xs { pos[out.len()] = *x; if *x > 4 { out.push(*x); }; }
pos.into_iter().map(|x| -> x.to_string()).join(",".to_string())
