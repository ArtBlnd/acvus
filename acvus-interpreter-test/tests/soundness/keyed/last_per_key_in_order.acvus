// expect 8,0,6,7
let xs = vec([4u64, 1u64, 8u64, 3u64, 6u64, 7u64]);
let h = vec([0u64, 0u64, 0u64, 0u64]);
for x in &xs {
    let b = *x % 4u64;
    let old = h[b];
    h[b] = if *x > 2u64 { *x } else { old };
}
h.into_iter().map(|x| -> x.to_string()).join(",".to_string())
