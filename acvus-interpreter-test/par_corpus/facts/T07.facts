for slice(r9) -> L1 else L2 stages [L1, L3]
// L1: free {const_str, take, call repeat, assign, ref, as_slice, call to_string, drop, assign, ref, as_slice, spawn print, eval, drop}
// L3: cycle Order(r11) any_order law(Order exact commutative) {merge}
// control upfront
// cost W=18298 O=18281 split when n > 32, n = len(r9)
