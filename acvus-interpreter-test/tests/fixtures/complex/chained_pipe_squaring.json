{
  "template": "% let x = as_iter(&@nums) | map(|n| -> *n) | filter(|n| -> *n > 0 && *n < 10) | map(|n| -> n * n) | collect\n{{ into_iter(x) | map(|i| -> i.to_string()) | join(\", \".to_string()) }}",
  "context": { "nums": [-5, 0, 3, 7, 12, 20] },
  "expected": "9, 49"
}
