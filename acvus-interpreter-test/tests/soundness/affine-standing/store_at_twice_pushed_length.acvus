// expect "5,0,3,0,8,0,1,0"
let xs = vec([5, 3, 8, 1]);
let out = vec([]);
let pos = vec([0, 0, 0, 0, 0, 0, 0, 0]);
for x in &xs { pos[out.len()] = *x; out.push(*x); out.push(*x); }
pos.into_iter().map(|x| -> x.to_string()).join(",".to_string())
