// refuse
// A map stage's next returns what its closure returns: the captured reference's lender is written while the pulled item is live.
let s = 5; let r = &s; let v = vec([1]); let m = into_iter(v) | map(|x| -> r); let a = next(&mut m); s = 6; *a.unwrap()
