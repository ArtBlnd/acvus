// expect 4
// `pass_later` is spawned lent `&s` and its result holds the loan; the result is read before `s` is written.
let s = "lent".to_string(); let r = pass_later(&s); let n = len(r); s = "x".to_string(); n
