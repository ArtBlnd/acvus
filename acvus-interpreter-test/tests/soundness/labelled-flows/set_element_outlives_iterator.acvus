// expect 5
// The element names the set's storage, not the iterator's: it is read after the iterator's block ends.
let s = hash_set(); insert(&mut s, 5); let a = { let it = as_iter(&s); next(&mut it) }; *a.unwrap()
