// expect 4
let acc = 0; for s in ["a".to_string(), "bb".to_string(), "c".to_string(), "dd".to_string(), "e".to_string()] { if len(&s) == 1 { continue; } acc = acc + len(&s); if acc > 3 { break; } } acc
