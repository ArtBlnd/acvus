// expect 9
// A map stage over a borrowed vector, its closure holding a reference, drained with no write.
let v = vec([1, 2, 3]); let w = vec([5]); let r = &w; let m = as_iter(&v) | map(|x| -> *x + len(r)); m | fold(0, |a, x| -> a + x)
