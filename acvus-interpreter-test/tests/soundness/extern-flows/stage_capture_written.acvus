// refuse
// The stage holds its closure, which captured a reference: that reference's lender is written while the stage is live.
let v = vec([1, 2, 3]); let w = vec([5]); let r = &w; let m = as_iter(&v) | map(|x| -> *x + len(r)); w = vec([9]); m | fold(0, |a, x| -> a + x)
