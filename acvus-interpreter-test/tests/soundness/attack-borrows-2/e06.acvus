// refuse
// a &mut String reaches a &str position while it is stored through in a later argument
let s = "abc" + ""; let r = &mut s; let f = |a, b| -> a.len() + b; f(r.as_str(), { *r = "x" + ""; 0u64 })
