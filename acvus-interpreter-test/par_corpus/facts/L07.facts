while r10 -> L1 else L2 stages [L0, L1, L5]
// L0: cycle Storage(r11)+Control in_order {ref, call next, test_variant, while}
// L1: free {unwrap_variant, assign, ref, as_slice, const_str, call contains, drop}
// L5: cycle Carried(r7) any_order law(Op(Add) exact commutative) {const, +, branch, jump}
// control chained through L0
// cost in place: no count on entry
