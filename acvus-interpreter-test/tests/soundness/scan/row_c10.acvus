// expect 0,0,1,1,1,2
let xs = vec([4, 2, 0, 5, 6, 0]);
let seg = 0;
let out = vec([]);
for x in &xs {
    if *x == 0 {
        seg = seg + 1;
    };
    out.push(seg);
}
out.into_iter().map(|x| -> x.to_string()).join(",".to_string())
