// refuse
// What a lambda writes through its &mut parameter holds the lender of its other argument.
let a = [1]; let b = [2]; let o = get(&a, 0u64); let f = |m, x| -> { *m = get(x, 0u64); 0 }; f(&mut o, &b); b = [5]; match o { Some(p) => *p, None => 0, }
