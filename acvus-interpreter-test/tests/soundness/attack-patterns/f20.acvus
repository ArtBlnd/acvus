// expect 2
// nested Result in list pattern on a call (no literal on the Err part)
let f = |u| -> [Ok(1), Err(u.to_string()), Ok(u)]; match f(2) { [Ok(1), Err(_b), Ok(3)] => 0, [Ok(_a), Err(_b), Ok(z)] => z, _ => 9, }
