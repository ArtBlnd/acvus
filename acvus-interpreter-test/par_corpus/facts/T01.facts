for slice(r11) -> L1 else L2 stages [L1, L3]
// L1: free {take, const, *, assign, ref, call to_string, assign, ref, as_slice, spawn print, eval, drop}
// L3: cycle Order(r13) any_order law(Order exact commutative) {merge}
// control upfront
// cost W=18295 O=18281 split when n > 32, n = len(r11)
