// refuse
// The element holds the deque's loan: a push while it is live is refused.
let d = deque(); push_back(&mut d, 5); let it = as_iter(&d); let a = next(&mut it); push_back(&mut d, 1); *a.unwrap()
