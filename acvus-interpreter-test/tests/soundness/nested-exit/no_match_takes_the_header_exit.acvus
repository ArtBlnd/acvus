// expect [99,99]
let find = |m| -> {
    for i in 0u64..m.len() {
        for j in 0u64..m[i].len() {
            if m[i][j] == 7 { return (i, j); };
        }
    }
    (99u64, 99u64)
};
find(vec([vec([1, 2]), vec([3]), vec([])]))
