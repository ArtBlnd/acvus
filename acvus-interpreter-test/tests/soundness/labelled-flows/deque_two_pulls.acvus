// expect 53
// Two pulls of one iterator over a deque, both elements read after the second (RFC-0096).
let d = deque(); push_back(&mut d, 5); push_back(&mut d, 3); push_back(&mut d, 8); let it = as_iter(&d); let a = next(&mut it); let b = next(&mut it); *a.unwrap() * 10 + *b.unwrap()
