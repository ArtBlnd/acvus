{
  "template": "{{ x = as_iter(&@users) | map(|u| -> clone(&u.name)) | collect }}{{ x | join(\", \") }}",
  "context": {
    "users": [
      { "name": "alice", "age": 30 },
      { "name": "bob", "age": 25 }
    ]
  },
  "expected": "alice, bob"
}
