// id: W16
// family: mut-write
// desc: each iteration reads and writes only slot `i` of one vec, via two statements
// expect: 11,7,17
let v = vec([5, 3, 8]);
for i in 0u64..v.len() {
    let t = v[i] * 2;
    v[i] = t + 1;
}
v.into_iter().map(|x| -> x.to_string()).join(",".to_string())
