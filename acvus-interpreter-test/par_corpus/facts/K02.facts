for slice(r25) -> L1 else L2 stages [L1, L3]
// L1: free {take, const, %, const}
// L3: cycle Storage(r28) keyed(r29) any_order law(Op(Add) exact commutative) {index, +, index_set}
// control upfront
// cost W=4 O=18281 split when n > 146248, n = len(r25)
