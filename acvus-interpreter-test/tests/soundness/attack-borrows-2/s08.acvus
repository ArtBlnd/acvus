// expect 5
// a spawn inside a loop reads the length the previous iteration pushed
let v = vec([1]); let i = 0; while i < 3 { let a = opaque_async(v.len() as i64); v.push(a); i = i + 1; } v[3u64] * 2 - 1
