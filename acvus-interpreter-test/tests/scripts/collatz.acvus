let n = @start;
let steps = 0;
let max_val = n;

while n > 1 {
    n = if n % 2 == 0 {
        n / 2
    } else {
        n * 3 + 1
    };
    steps = steps + 1;
    if n > max_val {
        max_val = n;
    };
}

max_val
