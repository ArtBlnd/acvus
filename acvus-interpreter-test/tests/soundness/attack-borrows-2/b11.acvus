// refuse
// a & joined from a &mut outlives a write to the &mut's owner in the next iteration
let a = vec([1]); let s = &a; let i = 0; while i < 2 { a.push(1); s = &a; i = i + 1; } 0; let q = &mut a; let t = if true { q } else { s }; a.push(0); t.len()
