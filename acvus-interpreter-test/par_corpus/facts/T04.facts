for slice(r7) -> L1 else L2 stages [L1, L3]
// L1: free {const_str, take, assign, ref, call to_string, assign, ref, concat, drop, assign, const_str, take, assign, ref, call to_string, assign, ref, concat, drop, assign, ref, as_slice, spawn print, ref, as_slice, spawn print, eval, drop, eval, drop}
// L3: cycle Order(r9) any_order law(Order exact commutative) {merge, merge}
// control upfront
// cost W=36642 O=18281 split when n > 16, n = len(r7)
