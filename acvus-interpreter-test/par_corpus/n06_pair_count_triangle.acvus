// id: N06
// family: nested
// desc: count pairs i<j with xs[i]+xs[j]==10 over a triangular nest
// expect: 3
let xs = vec([5, 3, 7, 5, 2, 8]);
let n = xs.len();
let c = 0;
for i in 0u64..n {
    for j in (i + 1u64)..n {
        if xs[i] + xs[j] == 10 {
            c = c + 1;
        };
    }
}
c
