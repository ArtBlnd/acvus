for slice(r15) -> L1 else L2 stages [L1, L5]
// L1: free {take, const, %, const, ==}
// L5: cycle Carried(r17) any_order law(Op(Add) exact commutative) {take, +, branch, jump}
// control upfront
// cost W=5 O=18281 split when n > 116999, n = len(r15)
