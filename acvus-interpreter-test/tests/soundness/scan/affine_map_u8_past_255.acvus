// expect attempt to multiply with overflow
let xs = vec([1u8, 1u8, 1u8, 1u8, 1u8, 1u8, 1u8, 1u8, 1u8]);
let y = 0u8;
let out = vec([]);
for x in &xs {
    y = 2u8 * y + *x;
    out.push(y);
}
out.len()
