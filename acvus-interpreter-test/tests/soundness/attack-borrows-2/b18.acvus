// refuse
// a join of a context-shared and a &mut; the &mut's owner written while the join lives
let v = vec([1]); let w = vec([2, 3]); let k = 1; let s = match k { 0 => &mut v, 1 => &w, _ => &v }; v.clear(); s.len()
