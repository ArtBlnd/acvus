// expect true
// converted argument &String passed where &str expected by a local closure calling contains
let f = |s, p| -> s.contains(p); let t = "hay".to_string(); let n = "a".to_string(); f(&t, &n)
