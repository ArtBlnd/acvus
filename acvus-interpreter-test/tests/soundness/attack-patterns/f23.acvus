// expect "abcabc"
// bound String stored into a Vec, then returned joined
let o = ("abc".to_string(), 1); let v = vec([]); match o { (s, 1) => { v.push(s); }, _ => { let z = 0; }, }; match o { (s, _) => { v.push(s); }, _ => { let z = 0; }, }; v[0u64].to_string() + &v[1u64]
