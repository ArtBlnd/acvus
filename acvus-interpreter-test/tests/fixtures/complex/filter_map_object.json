{
  "template": "{{ x = as_iter(&@products) | filter(|p| -> (*p).price >= 100) | map(|p| -> clone(&p.name)) | collect }}{{ x | join(\", \") }}",
  "context": {
    "products": [
      { "name": "apple", "price": 100 },
      { "name": "banana", "price": 50 },
      { "name": "cherry", "price": 200 }
    ]
  },
  "expected": "apple, cherry"
}
