// id: Z16
// family: negative
// desc: exponential smoothing `m = m * 0.5 + x` in floats; an affine law exists only with permission to reassociate
// expect: 5.5
let xs = vec([2.0, 4.0, 3.0]);
let m = 0.0;
for x in &xs {
    m = m * 0.5 + *x;
}
m
