// expect 130
let xss = [vec([1, -2]), vec([4, 5, 6]), vec([-7])]; let t = 0u64; for xs in &xss { let i = 0u64; let found = false; while i < xs.len() && !found { if xs[i] < 0 { found = true; } else { i = i + 1u64; }; } t = t * 10 + i; } t
