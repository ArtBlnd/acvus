for slice(r10) -> L1 else L2 stages [L1, L3]
// L1: free {take, assign, ref, call to_string, assign, ref}
// L3: cycle Storage(r11) in_order law(Op(Concat) exact) {ref, concat, assign}
// control upfront
