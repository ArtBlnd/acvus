// expect 2
// The write through the &mut parameter without the lender written.
let a = [1]; let b = [2]; let o = get(&a, 0u64); let f = |m, x| -> { *m = get(x, 0u64); 0 }; f(&mut o, &b); match o { Some(p) => *p, None => 0, }
