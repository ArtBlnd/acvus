// expect 5
// The element names the vector's storage, not the iterator's: it is read after the iterator's block ends.
let v = vec([5, 3]); let a = { let it = as_iter(&v); next(&mut it) }; *a.unwrap()
