// refuse
// A write through a &mut parameter is an output like a result: a loan on the lambda's own local may not reach it.
let a = [1]; let o = get(&a, 0u64); let f = |m| -> { let l = [3]; *m = get(&l, 0u64); 0 }; f(&mut o); match o { Some(p) => *p, None => 0, }
