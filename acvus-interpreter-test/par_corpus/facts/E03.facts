for slice(r7) -> L1 else L2 stages [L1, L3, L4]
// L1: free {take, assign, ref, call to_string, assign, ref, as_slice, spawn print, take, const, *}
// L3: cycle Storage(r10) in_order law(Fold(r10, #3) exact) {ref, call push}
// L4: cycle Order(r9) any_order law(Order exact commutative) {eval, merge}
// control upfront
