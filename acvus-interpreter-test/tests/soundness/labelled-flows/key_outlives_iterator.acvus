// expect 5
// The key names the map's storage, not the iterator's: it is read after the iterator's block ends.
let m = hash_map(); insert(&mut m, 5, 50); let a = { let it = keys(&m); next(&mut it) }; *a.unwrap()
