// refuse
// The element holds the vector's loan: a &mut of the vector while it is live is refused.
let v = vec([5, 3]); let it = as_iter(&v); let a = next(&mut it); let r = &mut v; push(r, 1); *a.unwrap()
